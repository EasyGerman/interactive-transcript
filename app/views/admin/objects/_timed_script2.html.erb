<h4>timed_script2.parsed_paragraphs</h4>
<% o.parsed_paragraphs.each_with_index do |parsed_paragraph, index| %>
  <h5>Paragraph <%= index + 1 %></h5>
  <% processed_paragraph = o.process_paragraph(parsed_paragraph) %>

  <h6>parsed_paragraph (txt)</h6>
  <pre><%= parsed_paragraph.to_txt %></pre>

  <h6>parsed_paragraph (json)</h6>
  <pre><%= JSON.pretty_generate(parsed_paragraph.as_json) %></pre>

  <h6>processed_paragraph.slices</h6>
  <table class="codes">
    <% processed_paragraph.slices.each do |slice| %>
      <tr>
        <td><code><%= slice[0] %></code></td>
        <td><code><%= slice[1] %></code></td>
        <td><code><%= slice[2] %></code></td>
      </tr>
    <% end %>
  </table>

  <h6>processed_paragraph (JSON)</h6>
  <pre><%= JSON.pretty_generate(processed_paragraph.as_json) %></pre>

  <h6>processed_paragraph.segments_as_plain_text (output)</h6>
  <small>(segments are generated by TimedScript::SplitProcessor)</small>
  <pre><%= processed_paragraph.segments_as_plain_text %></pre>
<% end %>
