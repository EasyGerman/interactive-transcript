<span class="paragraph-summary speaker-<%= paragraph.speaker.name.to_s.downcase %>">
  <%=
    link_to "#{paragraph.timestamp} #{paragraph.text.first(10)} #{"·" * (paragraph.text.length / 100)}",
      admin_episode_paragraph_path(episode.slug, paragraph.slug)
  %>
</span>
